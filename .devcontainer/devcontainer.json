{
  "name": "brig",
  "context": "..",
  "dockerFile": "./Containerfile",
  "containerEnv": {
    "APP_PATH": "${containerWorkspaceFolder}",
    "SHELL": "/bin/bash"
  },
  // TODO: Investigate how Visual Studio Code or the official CLI tool
  // deals with volumes if they don't yet exist; are they created
  // automatically?
  "mounts": [
    "source=go-cache,target=/root/go,type=volume"
  ]
}
